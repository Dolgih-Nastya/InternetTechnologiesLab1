<%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %>
      <%= f.hidden_field :user_id, :value => current_user.id%><br>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>